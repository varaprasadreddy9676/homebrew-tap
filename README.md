# DayTrail Homebrew Tap

Install [DayTrail](https://github.com/varaprasadreddy9676/DayTrail) on macOS:

```sh
brew tap varaprasadreddy9676/tap
brew install --cask daytrail
```

To update:

```sh
brew upgrade --cask daytrail
```
